/*
// "use client";
// import {createContext, ReactNode, useContext} from "react";
// import {useQuery} from "@tanstack/react-query";
// import authService from "@/service/auth.service";
//
//
// interface User {
//     id: number;
//     name: string;
//     role: "admin" | "vendor";
// }
//
// const AuthContext = createContext<{ user?: User; isLoading: boolean }>({isLoading: true});
//
// export const AuthProvider = ({children}: { children: ReactNode }) => {
//     const {data, isLoading} = useQuery(["authUser"], authService.me, {retry: false});
//     return <AuthContext.Provider value={{user: data, isLoading}}>{children}</AuthContext.Provider>;
// };
//
// export const useAuth = () => useContext(AuthContext);
*/
